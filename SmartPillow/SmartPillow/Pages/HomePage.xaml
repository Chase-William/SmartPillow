<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:skia="clr-namespace:SkiaSharp.Views.Forms;assembly=SkiaSharp.Views.Forms"
             xmlns:pancake="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView" 
             xmlns:viewModel="clr-namespace:SmartPillow.ViewModels;assembly=SmartPillowLib"
             mc:Ignorable="d"
             Title="Home"
             x:Class="SmartPillow.Pages.HomePage">

    <ContentPage.BindingContext>
        <viewModel:HomeViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.ToolbarItems>
        <!--Command="{Binding UserCommand}"-->
        <ToolbarItem IconImageSource="{Binding ProfileImage}" Clicked="Profile_Clicked"/>
    </ContentPage.ToolbarItems>

    <Grid>

        <skia:SKCanvasView x:Name="SKCanvas" PaintSurface="SKCanvas_PaintSurface"/>

        <Image Source="bedBackground.png" Aspect="Fill" VerticalOptions="CenterAndExpand"/>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.Resources>
                <ResourceDictionary>
                    <Style TargetType="pancake:PancakeView">
                        <Setter Property="CornerRadius" Value="14.5"/>
                        <Setter Property="BackgroundColor" Value="Black"/>
                        <Setter Property="Opacity" Value=".76"/>
                    </Style>
                </ResourceDictionary>
            </Grid.Resources>
            
            <pancake:PancakeView Margin="15,80,15,0"/>

            <pancake:PancakeView Margin="15,10,15,15" Grid.Row="1"/>
            
        </Grid>
        
    </Grid>

</ContentPage>
